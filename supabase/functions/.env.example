# Local-only secrets for `supabase functions serve`. Copy to .env (which is
# gitignored) and fill in. In production these are set with:
#   supabase secrets set SENDKIT_API_KEY=sk_...
SENDKIT_API_KEY=sk_xxxxxxxxxxxxxxxxxxxx
SENDKIT_FROM="ChipuRobo <noreply@chipurobo.com>"
