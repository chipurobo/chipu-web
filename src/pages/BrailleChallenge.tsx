// DEPRECATED — past hackathons are now rendered as archive cards inside
// the unified /hackathons page (Hackathons.tsx), which reads from
// src/data/hackathons.ts. To restore Braille Challenge as the current
// event, flip its `status` in the data file back to 'current'.
//
// This file remains only because the Mac filesystem mount prevents the
// editor from deleting it; safe to remove from version control.

const BrailleChallenge = () => null;
export default BrailleChallenge;
