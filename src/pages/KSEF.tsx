// DEPRECATED — KSEF is now a section inside the merged InclusiveRobotics
// page (/inclusive-robotics#ksef). This file remains as a stub only because
// the Mac filesystem mount prevents the editor from deleting it; it is no
// longer routed anywhere and is safe to remove from version control.

const KSEF = () => null;
export default KSEF;
