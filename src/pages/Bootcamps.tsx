// DEPRECATED — replaced by /microsoft-bootcamps (MicrosoftBootcamps.tsx).
// This stub remains only because the Mac filesystem mount prevents the
// editor from deleting it; safe to remove from version control.

const Bootcamps = () => null;
export default Bootcamps;
