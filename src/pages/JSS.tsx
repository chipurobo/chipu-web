// DEPRECATED — JSS content is now part of the KSEF section inside the
// merged InclusiveRobotics page (/inclusive-robotics#ksef). This file is
// a stub and is safe to remove from version control.

const JSS = () => null;
export default JSS;
