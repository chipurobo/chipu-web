// DEPRECATED — the "Programs" page is now the 3-stage page in
// InclusiveRobotics.tsx (routed at /programs via App.tsx). This file is
// never imported anywhere and is safe to delete in Finder.

const Programs = () => null;
export default Programs;
