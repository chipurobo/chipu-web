// DEPRECATED — the hackathon page is now /hackathons (Hackathons.tsx),
// which reads from src/data/hackathons.ts and renders whichever event
// is currently active. To update for a new hackathon, edit the data
// file, not this stub.
//
// This file remains only because the Mac filesystem mount prevents the
// editor from deleting it; safe to remove from version control.

const KeshoHackathon = () => null;
export default KeshoHackathon;
